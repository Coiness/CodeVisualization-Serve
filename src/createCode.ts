import { createInvitationCode } from "./service/invitationCodeService";

console.log(
  "==================================================================="
);
console.log(createInvitationCode());
console.log(
  "==================================================================="
);
