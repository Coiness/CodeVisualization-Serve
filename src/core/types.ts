export type Obj = {
  [key: string | number]: any;
};

/**
 * 文件名：types.ts
 * 描述：定义项目中使用的基本类型。
 *
 * 详细说明：
 * - 定义 `Obj` 类型：
 *   - 描述：一个通用的对象类型，其键可以是字符串或数字，值可以是任意类型。
 *
 * 功能调用情况：
 * - 主要用于描述和约束其他模块中的对象结构，确保类型一致性。
 *
 * 使用示例：
 * ```typescript
 * import { Obj } from "./types";
 *
 * const example: Obj = {
 *   name: "张三",
 *   age: 30,
 *   hobbies: ["阅读", "运动"]
 * };
 * ```
 *
 * 注意事项：
 * - `Obj` 类型非常宽泛，使用时需要注意类型安全，尽量避免未经检查的类型转换或值的使用。
 * - 可根据项目需求扩展或细化类型定义，以提高类型的精确性和安全性。
 */
